# 🎁 GiftHarbor

**GiftHarbor** — это социальный вишлист для подарков с возможностью совместного сбора средств в реальном времени.

🔹 Создавайте списки желаний  
🔹 Делитесь ссылкой с друзьями  
🔹 Резервируйте подарки и скидывайтесь  
🔹 Владелец не видит, кто что выбрал — сюрприз сохраняется  
🔹 Всё обновляется мгновенно (WebSockets)

---

## 🧠 Наши продуктовые решения

Мы не просто сделали вишлист — мы продумали каждый сценарий:

| Решение | Описание |
|--------|----------|
| **🔐 Публичный доступ без регистрации** | Любой может посмотреть вишлист по ссылке. Регистрация нужна только для действий (резервация, вклад). |
| **🎭 Анонимность дарителей** | Владелец видит только факт резервации и сумму сбора, но не имена — сюрприз сохраняется. |
| **🛡️ Мягкое удаление товаров** | Если товар удалён, но на него уже скинулись — запись остаётся в БД, но скрыта из вишлиста. |
| **💰 Минимальный вклад $10** | Психологически низкий порог для старта сбора. |
| **📱 Адаптивный дизайн** | Уютно выглядит на всех устройствах — от телефона до большого монитора. |
| **🤖 AI-автозаполнение** | Вставь ссылку на товар — и мы сами подтянем название, цену и картинку. |

---

## 🛠️ Стек технологий

| Часть | Технологии |
|-------|-----------|
| **Фронтенд** | Next.js 15, TypeScript, TailwindCSS, shadcn/ui |
| **Бэкенд** | FastAPI, SQLAlchemy, Pydantic, JWT, WebSockets |
| **База данных** | PostgreSQL (Supabase) |
| **Деплой** | Vercel (фронтенд), Railway (бэкенд) |

---

## 🚀 Как запустить проект локально

### Предварительные требования
- Node.js 18+
- Python 3.12+
- Git

### 1. Клонировать репозиторий
```bash
git clone https://github.com/Valerson7/gift-harbor.git
cd gift-harbor

2. Настроить бэкенд
bash
cd backend
python -m venv venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

pip install -r requirements.txt
Создайте файл .env в папке backend:

env
DATABASE_URL="postgresql://..."
Запустите сервер:

bash
uvicorn app.main:app --reload --port 8080
Бэкенд будет доступен на http://localhost:8080

3. Настроить фронтенд
bash
cd frontend
npm install
npm run dev
Фронтенд будет доступен на http://localhost:3000

🌐 Демо
Фронтенд: https://gift-harbor.vercel.app

Бэкенд API: https://gift-harbor-backend.railway.app

📄 Лицензия
MIT © Валерий Квятецкий

text

---

# 🚀 ЧАСТЬ 3. ДЕПЛОЙ (ВЫКЛАДЫВАЕМ В ИНТЕРНЕТ)

## ✅ ШАГ 1. ДЕПЛОЙ ФРОНТЕНДА НА VERCEL

1. Перейди на [https://vercel.com](https://vercel.com)
2. Нажми **"Sign Up"** → выбери **"Continue with GitHub"**
3. Нажми **"Add New Project"**
4. Выбери репозиторий **`gift-harbor`**
5. В поле **"Root Directory"** выбери **`frontend`**
6. Нажми **"Deploy"**

✅ Через 2 минуты получишь ссылку: `https://gift-harbor.vercel.app`

---

## ✅ ШАГ 2. ДЕПЛОЙ БЭКЕНДА НА RAILWAY

1. Перейди на [https://railway.app](https://railway.app)
2. Нажми **"Start a New Project"** → **"Deploy from GitHub"**
3. Выбери репозиторий **`gift-harbor`**
4. Добавь переменные окружения в разделе **"Variables"**:
   - `DATABASE_URL` = строка из Supabase
   - `JWT_SECRET` = любой секретный ключ (например, `my-super-secret-key-2026`)
   - `SMTP_USER` = `valersonik23@gmail.com`
   - `SMTP_PASSWORD` = твой пароль приложения
5. Нажми **"Deploy"**

✅ Через пару минут получишь ссылку: `https://gift-harbor-backend.up.railway.app`

---

## ✅ ШАГ 3. ОБНОВИ ФРОНТЕНД

В файле `frontend/lib/api.ts` замени:
```typescript
const API_URL = 'http://localhost:8080';
на:

typescript
const API_URL = 'https://gift-harbor-backend.up.railway.app';
Закоммить и запуши:

powershell
cd C:\Users\Валерий\Documents\gift-harbor
git add .
git commit -m "🚀 change API URL to production"
git push
Vercel автоматически перезапустит фронтенд.